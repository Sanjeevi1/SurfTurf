const config = {
    initialMessages: [{ type: "text", id: 1, message: "Hi! How can I assist you with turf booking today?" }],
    botName: "TurfBot",
    customStyles: {
        botMessageBox: {
            backgroundColor: "#376B7E",
        },
        chatButton: {
            backgroundColor: "#5ccc9d",
        },
    },
};

export default config;
